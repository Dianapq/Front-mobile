Framework: React 18 (Vite)
Routing: React Router DOM v6
HTTP: Axios

##Instalación
    bashnpm install
    npm run dev

Autenticación
El login usa POST /auth/login-cobrador. 
El token JWT se guarda en localStorage con la clave token y se adjunta manualmente en el header de cada petición

